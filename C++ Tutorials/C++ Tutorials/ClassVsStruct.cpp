#include <iostream>

#define LOG(x) std::cout << x << std::endl

/*

//ONLY DIFFERENCE: Structs have default public members, Classes have default private members
//Use Structs: plain old data or POD, a structure that holds primiatives together, example: Vector2D
struct Vector2D {
	float x;
	float y;
};
//Write functions that a very concernated on only the variables. Used for smaller types
// Classes: Complex stuff that relies on inhertiance 
// Structs: Simple types that build off of primitives


int main() {
	std::cin.get();
}
*/