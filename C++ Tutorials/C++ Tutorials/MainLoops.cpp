#include <iostream>

/*
int main() {
	// for (declartion or nothing; condition; execute once loop is finished or nothing) {}
	for (int i = 0; i < 5; i++) {
		std::cout << "Hello World!" << std::endl;
	}

	//while (condition) { } // continue to do until the condition is not met

	// do { } while (condtion); // does what is in the block once, and continues to do it if the condition is met
	std::cin.get();

	//return = exit the entire function
	//continue = only used inside the loop, go to next iteration of loop
	//break = in loop/switch statement, end the entire loop
}
*/