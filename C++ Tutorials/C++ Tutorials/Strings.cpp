#include <iostream>
#include <string>

/*
void PrintString(const std::string& string) {
	//use const reference to the string, so the string isn't cloned, and the string isn't modified
	std::cout << string << std::endl;
}

int main() {
	const char* exString = "Example String"; //Anything in double quotes returns a char pointer
	// "a" -> A String literal with 'a' and null terminator
	// 'a' -> a single character literal
	char name[5] = { 'A', 'l', 'e', 'x', '\0' }; // '\0' = null terminator
	
	std::string RealString = "Hello!"; //Use this, gives more functionality, plenty of methods
}
*/