#include "Entity.h"

/*
int main() {
	Entity ent(10, 5, "Ball"); 
	
	//Use New keyword when you want an object to remain in existence until specifcally deleted
	Entity* e = new Entity(); //New keyword allocates memory for size of type, and then returns a pointer to that memory
	e->Print(); // -> for using methods on an object that isn't a object, but rather the object's pointer
	//Use . for variables that are the object, not the pointer
	delete e; // MUST CALL DELETE, calls destructor as well
	ent.Print();

	Player* ply = new Player("Player1");
	PrintClassName(ply);
	PrintClassName(e);
	std::cin.get();
}
*/
